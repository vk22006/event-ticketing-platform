<nav class="navbar">
    <div class="container">
        <div class="navbar__inner">
            <a href="/events" class="navbar__brand">
                <span class="navbar__brand-icon">🎫</span>
                Event<span>Pro</span>
            </a>

            <ul class="navbar__nav">
                <li><a href="/events">Discover</a></li>
                <% if (user && user.role==='organizer' ) { %>
                    <li><a href="/events/create">Create Event</a></li>
                    <% } %>
                        <% if (user && user.role==='admin' ) { %>
                            <li><a href="/admin/dashboard">Admin</a></li>
                            <% } %>
            </ul>

            <div class="navbar__actions">
                <% if (user) { %>
                    <span class="role-badge role-badge--<%= user.role %>">
                        <%= user.role %>
                    </span>
                    <a href="/dashboard" class="btn btn-ghost btn-sm">Dashboard</a>
                    <a href="/auth/logout" class="btn btn-outline btn-sm">Logout</a>
                    <% } else { %>
                        <a href="/auth/login" class="btn btn-ghost   btn-sm">Login</a>
                        <a href="/auth/register" class="btn btn-primary btn-sm">Sign Up</a>
                        <% } %>
            </div>
        </div>
    </div>
</nav>